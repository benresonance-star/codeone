Generated runtime artifacts for Phase B verification live here.

- `benchmarks/`: extractor comparison outputs, including Docling vs `pdfplumber`.
- `smoke/`: end-to-end API smoke test outputs.

The JSON files in these folders are generated by the tools under `backend/tools/` and are ignored by git.
